<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user  %>
    <p>
      <%= link_to "Show this user's articles", "/users/#{user.id}/articles" %>
    </p>
  <% end %>
</div>

<%# <%= link_to "See search analytics", "/users/#{@user.id}/anals" %> 


